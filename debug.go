package graphman

/* uncomment to use (else, deadcode)
func debugGraph(g *Graph) string {
	output := ""
	for _, edge := range g.Edges() {
		output += fmt.Sprintf("- edge: %s\n", edge)
	}
	for _, vertex := range g.Vertices() {
		output += fmt.Sprintf("- vertex: %s\n", vertex)
	}
	return output
}
*/
